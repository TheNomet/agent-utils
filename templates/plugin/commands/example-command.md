---
description: One-line summary shown in the slash-command picker.
argument-hint: "[optional args]"
---

# /example-command

Describe what this command does. The body of this file becomes the prompt
sent to Claude when the user runs `/example-command`.

Use `$ARGUMENTS` to reference whatever the user passed after the command.

## Steps

1. ...
2. ...
